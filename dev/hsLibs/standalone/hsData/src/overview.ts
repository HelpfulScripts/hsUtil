/**
 * # hsData
 * 
 * Helpful Scripts data management functions that are framework independent. 
 * 
 * ## Data Types
 * - &nbsp; {@link NumRange NumRange} defines a single [min, max] numeric range.
 * - &nbsp; {@link NumDomain NumDomain} defines a numeric domain that includes all values of a column
 * - &nbsp; {@link DateDomain DateDomain} defines a Date domain that includes all values of a column
 * - &nbsp; {@link NameDomain NameDomain} defines a categorical domain that includes all values of a column
 * - &nbsp; {@link Domain Domain} defines a generic domain that can be any of the typed domains.
 * - &nbsp; {@link ColSpecifier ColSpecifier} defines a Column Specifier, either as column name or index in the {@link Data.DataRow `DataRow`} array 
 * - &nbsp; {@link DataVal DataVal} a generic data value type, used in the {@link Data.DataRow `DataRow`} array
 * - &nbsp; {@link DataRow DataRow} a single row of column values
 * 
 * ## Data Class
 * - &nbsp; {@link Data Data} A simple database object, featuring named columns and filtering functions
 */

 /** */

 
